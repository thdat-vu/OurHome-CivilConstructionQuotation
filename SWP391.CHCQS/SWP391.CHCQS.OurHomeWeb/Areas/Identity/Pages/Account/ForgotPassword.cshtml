@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Quên mật khẩu?";
}

<h1 class="d-flex justify-content-center align-items-center">@ViewData["Title"]</h1>
<p>Vui lòng điền email của bạn, kiểm tra xác minh đầy đủ và nhập mật khẩu mới</p>
<hr />
<h6 class="container d-flex justify-content-center">Điền email của bạn</h6>
<div class="container d-flex justify-content-center align-items-center min-vh-30 mb-3">
    <div class="row rounded-3 p-3" style="width: 50%">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" />
                        <label asp-for="Input.Email" class="form-label"></label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <button type="submit" class="w-100 btn btn-lg btn-primary">Đặt lại mật khẩu</button>
                </form>
        </div>
    </div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
